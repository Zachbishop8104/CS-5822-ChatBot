services:
  app:
    build: .
    volumes:
      - .:/ChatBot
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]